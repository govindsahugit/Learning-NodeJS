// imported packages are Library Packages these Packages are also called Local Packages
import axios from "axios";
console.log(axios);

// Packages we use in through terminal are CLI Packages like a popular Library Vite, when we install these packages we get .bin file in our node_modules

// Global Packages are packages we install globally in our system using -g we can access these packages from any where in our system.